import { Typography } from '@mui/material';

/** Placeholder - replaced by the operations build. */
export default function OperationsPage() {
  return <Typography variant="h5">Operations</Typography>;
}
