/**
 * The application theme. Built from the design tokens; see
 * shared/design-system/README.md for how to use it and what it exposes.
 */
import { createAppTheme } from '@/shared/design-system/createAppTheme';

export const theme = createAppTheme();
